import { Footer } from '@/modules/Footer';
import { HeaderServer } from '@/modules/Header';

const Home = async () => (
  <main>
    <HeaderServer />
    <Footer />
  </main>
);

export default Home;
